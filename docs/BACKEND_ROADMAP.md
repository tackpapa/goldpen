# ClassFlow OS 백엔드 구축 로드맵

> **상세 계획**: [docs/pdca/backend-implementation/plan.md](./pdca/backend-implementation/plan.md)

---

## 📊 진행 현황

| Phase | 작업 | 진행률 | 예상 시간 | 상태 |
|-------|------|--------|-----------|------|
| 0️⃣ | 데이터베이스 설계 | 100% | ✅ 완료 | ✅ |
| 1️⃣ | 인증 & 인가 시스템 | 0% | 14h | 🔜 |
| 2️⃣ | 핵심 CRUD API | 0% | 29h | ⏳ |
| 3️⃣ | 자동화 엔진 기본 | 0% | 7h | ⏳ |
| 4️⃣ | 보안 & 에러 처리 | 0% | 7h | ⏳ |
| 5️⃣ | 테스트 | 0% | 16h | ⏳ |
| 6️⃣ | 문서화 & 배포 | 0% | 10h | ⏳ |

**전체 진행률**: 12% (데이터베이스만 완료)
**예상 총 소요 시간**: 83시간 (~3주)

---

## 🎯 3주 마일스톤

### Week 1: 인증 & 핵심 API (Foundation)
**목표**: 로그인하여 학생/반/출결 데이터를 조회/수정할 수 있다

**주요 산출물**:
- ✅ 회원가입/로그인 API
- ✅ JWT 인증 미들웨어
- ✅ RBAC 권한 시스템
- ✅ 학생 CRUD API
- ✅ 반 CRUD API
- ✅ 출결 기록 API
- ✅ 상담 신청 API

**검증 방법**:
```bash
# 1. 회원가입
curl -X POST http://localhost:8000/api/auth/register \
  -d '{"email":"admin@test.com","password":"12345678","name":"관리자","role":"owner"}'

# 2. 로그인
curl -X POST http://localhost:8000/api/auth/login \
  -d '{"email":"admin@test.com","password":"12345678"}'
# → { "token": "eyJhbGc..." }

# 3. 학생 목록 조회
curl -H "Authorization: Bearer TOKEN" \
  http://localhost:8000/api/students

# 4. 학생 등록
curl -X POST -H "Authorization: Bearer TOKEN" \
  http://localhost:8000/api/students \
  -d '{"name":"김철수","studentNumber":"2024-001",...}'
```

---

### Week 2: 자동화 & 보안 (Enhancement)
**목표**: 상담 신청 시 이메일이 자동 발송되고, API가 안전하게 보호된다

**주요 산출물**:
- ✅ 이메일 알림 시스템
- ✅ 출결 자동 체크
- ✅ 입력 검증 강화 (Zod)
- ✅ Rate Limiting
- ✅ 에러 처리 표준화
- ✅ API 통합 테스트 (70% coverage)

**검증 방법**:
- 상담 신청 → 이메일 수신 확인
- 로그인 5회 실패 → 15분 잠금 확인
- 잘못된 입력 → 400 에러 + 명확한 메시지
- `npm test` → 모든 테스트 통과

---

### Week 3: 품질 & 배포 (Production Ready)
**목표**: 프로덕션 배포 가능한 품질 확보

**주요 산출물**:
- ✅ 시험 관리 API (선택)
- ✅ 과제 관리 API (선택)
- ✅ API 문서 자동 생성 (Swagger)
- ✅ E2E 테스트 (주요 플로우)
- ✅ Cloudflare Pages 배포 설정
- ✅ CI/CD 파이프라인

**검증 방법**:
- `/api/docs` → Swagger UI 정상 표시
- E2E 테스트 통과
- 프로덕션 배포 성공
- OWASP Top 10 체크리스트 검증

---

## 🏗️ 아키텍처 개요

```
┌─────────────────────────────────────────────────────┐
│                   Frontend (Next.js)                │
│  (23 Pages + 27 UI Components - Already Built)     │
└────────────────┬────────────────────────────────────┘
                 │
                 │ HTTP/JSON
                 ▼
┌─────────────────────────────────────────────────────┐
│              API Layer (Next.js API Routes)         │
│  ┌──────────────────────────────────────────────┐   │
│  │  Middleware                                  │   │
│  │  - JWT Verification                          │   │
│  │  - RBAC Authorization                        │   │
│  │  - Rate Limiting                             │   │
│  │  - Error Handling                            │   │
│  └──────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────┐   │
│  │  API Routes                                  │   │
│  │  /api/auth/*      - Authentication           │   │
│  │  /api/students/*  - Student Management       │   │
│  │  /api/classes/*   - Class Management         │   │
│  │  /api/attendance/* - Attendance Recording    │   │
│  │  /api/consultations/* - Consultation         │   │
│  └──────────────────────────────────────────────┘   │
└────────────────┬────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────┐
│           Business Logic Layer (Services)           │
│  ┌──────────────────────────────────────────────┐   │
│  │  lib/services/                               │   │
│  │  - student.service.ts                        │   │
│  │  - class.service.ts                          │   │
│  │  - attendance.service.ts                     │   │
│  │  - email.service.ts                          │   │
│  │  - automation.service.ts                     │   │
│  └──────────────────────────────────────────────┘   │
└────────────────┬────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────┐
│            Data Access Layer (Drizzle ORM)          │
│  ┌──────────────────────────────────────────────┐   │
│  │  lib/db/                                     │   │
│  │  - index.ts (DB connection)                  │   │
│  │  - schema/*.ts (10 tables)                   │   │
│  └──────────────────────────────────────────────┘   │
└────────────────┬────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────┐
│          Database (PostgreSQL 16)                   │
│  ┌──────────────────────────────────────────────┐   │
│  │  Docker Container (Development)              │   │
│  │  Neon/Supabase (Production)                  │   │
│  └──────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────┘

          External Services (Future)
┌────────────┬───────────────┬──────────────┐
│  SendGrid  │   KakaoTalk   │   OpenAI     │
│  (Email)   │   (알림톡)     │  (GPT-4o)    │
└────────────┴───────────────┴──────────────┘
```

---

## 📋 우선순위별 API 목록

### P0 - Must Have (Week 1)

**인증** (3 endpoints)
- `POST /api/auth/register` - 회원가입
- `POST /api/auth/login` - 로그인
- `GET /api/auth/me` - 현재 사용자 정보

**학생 관리** (5 endpoints)
- `GET /api/students` - 학생 목록
- `GET /api/students/:id` - 학생 상세
- `POST /api/students` - 학생 등록
- `PUT /api/students/:id` - 학생 수정
- `DELETE /api/students/:id` - 학생 삭제

**반 관리** (5 endpoints)
- `GET /api/classes` - 반 목록
- `POST /api/classes` - 반 생성
- `POST /api/classes/:id/enroll` - 수강 등록
- `DELETE /api/classes/:id/enroll/:enrollmentId` - 수강 취소
- `GET /api/classes/:id/students` - 반 학생 목록

**출결 관리** (3 endpoints)
- `POST /api/attendance` - 출결 기록
- `GET /api/attendance` - 출결 조회
- `GET /api/attendance/stats` - 출결 통계

**상담 관리** (3 endpoints)
- `POST /api/consultations` - 상담 신청 (공개)
- `GET /api/consultations` - 상담 목록
- `PUT /api/consultations/:id` - 상담 상태 변경

**Total P0**: 19 endpoints

---

### P1 - Should Have (Week 2)

**강사 관리** (4 endpoints)
- `GET /api/teachers` - 강사 목록
- `POST /api/teachers` - 강사 등록
- `PUT /api/teachers/:id` - 강사 수정
- `DELETE /api/teachers/:id` - 강사 삭제

**자동화** (2 endpoints)
- `POST /api/notifications/email` - 이메일 발송
- `POST /api/attendance/check-in` - 출석 체크인

**Total P1**: 6 endpoints

---

### P2 - Nice to Have (Week 3)

**시험 관리** (4 endpoints)
- `GET /api/exams` - 시험 목록
- `POST /api/exams` - 시험 생성
- `POST /api/exams/:id/results` - 성적 입력
- `GET /api/exams/:id/results` - 성적 조회

**과제 관리** (4 endpoints)
- `GET /api/homework` - 과제 목록
- `POST /api/homework` - 과제 생성
- `POST /api/homework/:id/submit` - 과제 제출
- `GET /api/homework/:id/submissions` - 제출 현황

**Total P2**: 8 endpoints

---

**총 API 개수**: 33 endpoints (P0: 19, P1: 6, P2: 8)

---

## 🛠️ 기술 스택

### Backend
- **Framework**: Next.js 14 API Routes
- **ORM**: Drizzle ORM (TypeScript Native)
- **Database**: PostgreSQL 16 (Docker → Neon)
- **Authentication**: JWT + bcrypt
- **Validation**: Zod

### DevOps
- **Development**: Docker Compose
- **CI/CD**: GitHub Actions
- **Deployment**: Cloudflare Pages
- **Monitoring**: Sentry (에러 트래킹)

### Testing
- **Unit**: Vitest
- **Integration**: Supertest
- **E2E**: Playwright

---

## 📚 디렉토리 구조 (예상)

```
classflow-os/
├── app/
│   └── api/                    # API Routes
│       ├── auth/
│       │   ├── register/route.ts
│       │   ├── login/route.ts
│       │   └── me/route.ts
│       ├── students/
│       │   ├── route.ts       # GET /api/students, POST
│       │   └── [id]/route.ts  # GET, PUT, DELETE
│       ├── classes/
│       ├── attendance/
│       └── consultations/
├── lib/
│   ├── auth/                   # 인증 유틸리티
│   │   ├── jwt.ts
│   │   ├── password.ts
│   │   ├── middleware.ts
│   │   └── rbac.ts
│   ├── services/               # 비즈니스 로직
│   │   ├── student.service.ts
│   │   ├── class.service.ts
│   │   ├── attendance.service.ts
│   │   ├── email.service.ts
│   │   └── automation.service.ts
│   ├── validations/            # Zod 스키마
│   │   ├── auth.ts
│   │   ├── student.ts
│   │   └── class.ts
│   ├── errors/                 # 에러 클래스
│   │   └── index.ts
│   └── middleware/             # 공통 미들웨어
│       ├── rate-limit.ts
│       └── error-handler.ts
└── tests/
    ├── unit/                   # 단위 테스트
    ├── integration/            # API 통합 테스트
    └── e2e/                    # E2E 테스트
```

---

## ⚡ 빠른 시작 (Quick Start)

### 1. 환경 설정
```bash
# 1. Docker PostgreSQL 시작
npm run docker:up

# 2. 마이그레이션 실행
npm run db:push

# 3. 환경 변수 설정
# .env.local 파일 확인 및 수정
# JWT_SECRET 생성: openssl rand -base64 32
```

### 2. 개발 서버 실행
```bash
npm run dev
# → http://localhost:8000
```

### 3. API 테스트
```bash
# 회원가입
curl -X POST http://localhost:8000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"email":"test@test.com","password":"12345678","name":"테스트","role":"owner"}'

# 로그인
curl -X POST http://localhost:8000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"test@test.com","password":"12345678"}'
```

---

## 🔗 관련 문서

- **상세 계획**: [docs/pdca/backend-implementation/plan.md](./pdca/backend-implementation/plan.md)
- **아키텍처**: [ARCHITECTURE.md](../ARCHITECTURE.md)
- **개발 가이드**: [CLAUDE.md](../CLAUDE.md)
- **데이터베이스 설정**: [docs/DATABASE_SETUP.md](./DATABASE_SETUP.md)

---

**작성일**: 2025-11-18
**버전**: 0.1.0
**작성자**: PM Agent
